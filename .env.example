# ============================================================================
# HNS IT Center — Next.js Environment Variables
# ============================================================================
# Salin file ini menjadi `.env.local` untuk development.
# JANGAN commit `.env.local` ke Git.
# Detail lengkap tiap variabel ada di docs/07-environment-variables.md
# ============================================================================


# ----------------------------------------------------------------------------
# [WAJIB] WooCommerce
# ----------------------------------------------------------------------------
# URL base WordPress (TANPA /wp-json di akhir)
WOOCOMMERCE_URL=https://hnsitcenter.id

# API key dari: wp-admin -> WooCommerce -> Settings -> Advanced -> REST API
WOOCOMMERCE_CONSUMER_KEY=ck_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WOOCOMMERCE_CONSUMER_SECRET=cs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# ----------------------------------------------------------------------------
# [WAJIB] Site
# ----------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=HNS IT Center

# Hostname image WordPress (dipakai next/image remotePatterns)
NEXT_PUBLIC_IMAGE_DOMAIN=hnsitcenter.id


# ----------------------------------------------------------------------------
# [WAJIB] Webhook Revalidation
# ----------------------------------------------------------------------------
# Generate: openssl rand -hex 32
# Nilai ini juga dimasukkan sebagai "Secret" di WooCommerce -> Webhooks
REVALIDATE_SECRET=change-me-with-random-32-chars-or-more


# ----------------------------------------------------------------------------
# [WAJIB] WhatsApp CS
# ----------------------------------------------------------------------------
# Format internasional TANPA tanda + atau 00 (contoh: 62821xxxxxxx)
NEXT_PUBLIC_WHATSAPP_CS_NUMBER=628xxxxxxxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Payment Gateway — Midtrans
# Aktifkan setelah checkout online siap. Fase 1 bisa pakai WA order dulu.
# ----------------------------------------------------------------------------
# MIDTRANS_SERVER_KEY=SB-Mid-server-xxxxx
# MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxx
# NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxx
# MIDTRANS_IS_PRODUCTION=false


# ----------------------------------------------------------------------------
# [OPSIONAL] Payment Gateway — Xendit (alternatif Midtrans)
# ----------------------------------------------------------------------------
# XENDIT_SECRET_KEY=xnd_development_xxxxx
# XENDIT_WEBHOOK_TOKEN=xxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Search — Meilisearch (Fase 2)
# Aktifkan setelah katalog besar & butuh search cepat + faceted
# ----------------------------------------------------------------------------
# MEILISEARCH_HOST=http://localhost:7700
# MEILISEARCH_API_KEY=masterKey
# NEXT_PUBLIC_MEILISEARCH_HOST=http://localhost:7700
# NEXT_PUBLIC_MEILISEARCH_SEARCH_KEY=search-only-key-xxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Analytics & Marketing
# ----------------------------------------------------------------------------
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_FB_PIXEL_ID=xxxxxxxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Error Tracking — Sentry
# ----------------------------------------------------------------------------
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Email SMTP — verifikasi akun & reset password pelanggan
# (daftar manual di /register), sekaligus tersedia untuk service booking
# notif/order confirmation nanti. Tanpa ini, daftar/lupa-password membalas
# error yang jelas, sisa aplikasi tetap jalan normal.
#
# PILIHAN YANG DIPAKAI PRODUKSI: SMTP Hostinger, mengirim sebagai
# noreply@hnsitcenter.id. Dipilih karena SPF domainnya sudah siap dan
# alamat pengirimnya memakai domain HNS sendiri — bukan alamat Gmail
# pribadi yang tampak asing di kotak masuk pelanggan.
#
# SMTP_PORT=465 → TLS sejak koneksi dibuka. Tidak ada saklar SSL/TLS
# terpisah: lib/email/send.ts menurunkannya dari nomor port
# (`secure: port === 465`). Port 587 juga hidup dan memakai STARTTLS.
#
# SMTP_USER harus alamat LENGKAP (noreply@hnsitcenter.id), bukan
# "noreply" saja. SMTP_PASSWORD adalah password MAILBOX yang dibuat di
# hPanel (Emails → Email Accounts), BUKAN password akun hPanel.
#
# SMTP_FROM harus memakai alamat yang sama dengan SMTP_USER. Boleh
# ditambah nama tampilan: "Nama <alamat@sama-dengan-SMTP_USER>".
#
# EMAIL_REPLY_TO opsional — arahkan balasan pelanggan ke alamat yang
# benar-benar dipantau (CS), bukan ke SMTP_USER yang cuma dipakai kirim.
#
# ALTERNATIF LAMA (Gmail, dipakai selama pengembangan): SMTP_HOST=
# smtp.gmail.com, SMTP_PORT=587, SMTP_USER=akun Gmail, SMTP_PASSWORD=
# App Password 16 karakter tanpa spasi (myaccount.google.com/apppasswords,
# wajib 2FA). Batasnya: Gmail MENIMPA header From ke alamat yang login,
# jadi noreply@hnsitcenter.id tidak bisa dipakai dari sana — itu sebabnya
# produksi memakai SMTP Hostinger. Lihat docs/07 §2.6.
# ----------------------------------------------------------------------------
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_USER=noreply@hnsitcenter.id
# SMTP_PASSWORD=xxxxxxxxxxxxxxxx
# SMTP_FROM="HNS IT Center <noreply@hnsitcenter.id>"
# Alamat CS yang BENAR-BENAR dipantau, untuk balasan pelanggan.
#
# JANGAN pakai cs@hnsitcenter.co.id — domain `.co.id` itu TIDAK TERDAFTAR
# (diperiksa 13 Agustus 2026 lewat resolver publik: "Non-existent domain"),
# jadi balasan pelanggan ke sana memantul dan tidak pernah sampai ke siapa pun.
# Domain yang punya mail aktif adalah `hnsitcenter.id` (MX Hostinger).
# EMAIL_REPLY_TO=cs@hnsitcenter.id


# ----------------------------------------------------------------------------
# [WAJIB untuk Admin Panel] Auth — kunci penanda tangan cookie sesi
# ----------------------------------------------------------------------------
# Tanpa ini login admin tidak bisa jalan. Minimal 32 karakter.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# JANGAN pakai nilai yang sama antara development dan produksi — mengganti nilai
# ini membatalkan seluruh sesi yang sedang berjalan, yang justru berguna kalau
# suatu saat perlu memaksa semua orang login ulang.
AUTH_SECRET=

# AUTH_URL tidak dipakai. Sesi admin memakai cookie bertanda tangan sendiri,
# bukan Auth.js, jadi tidak ada URL callback yang perlu dikonfigurasi.


# ----------------------------------------------------------------------------
# [OPSIONAL] Akun Pelanggan — Google OAuth
# Login pelanggan (/login) — simpan rakitan PC, riwayat pesanan. Setup
# lengkap: docs/09-google-oauth-setup.md. Tanpa ini /api/auth/google
# membalas error yang jelas, sisa aplikasi tetap jalan normal.
# ----------------------------------------------------------------------------
# GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Admin Panel — WordPress Application Password
# Dipakai buat upload gambar produk lewat admin panel (WordPress Media REST
# API, /wp-json/wp/v2/media) — BEDA kredensial dari WOOCOMMERCE_CONSUMER_KEY/
# SECRET di atas (yang cuma valid untuk /wp-json/wc/v3/).
# Setup: wp-admin -> Users -> Profile -> Application Passwords -> generate.
# ----------------------------------------------------------------------------
# WORDPRESS_APP_USER=admin
# WORDPRESS_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Admin Panel — Database (Prisma + MariaDB Hostinger)
# Dipakai buat data non-produk yang dikelola admin panel (toko/lokasi, konten
# kebijakan/FAQ). Belum wajib untuk storefront utama berjalan (ada fallback).
# Setup: buat database MariaDB di hPanel Hostinger, lalu:
#   npx prisma migrate deploy
#   npx prisma db seed
# JANGAN `migrate dev` atau `db push` — dua-duanya dilarang di repo ini,
# alasannya di docs/08-database-migrations.md.
# ----------------------------------------------------------------------------
# DATABASE_URL="mysql://user:password@host:3306/dbname"


# ----------------------------------------------------------------------------
# [WAJIB untuk Admin Panel] Cloudflare R2 — penyimpanan gambar produk
# SATU-SATUNYA jalur upload foto produk. WordPress Media sudah TIDAK dipakai
# (lihat catatan di blok WordPress di atas). Tanpa kelimanya, upload menolak
# dengan pesan jelas dan sisa aplikasi tetap jalan normal. Rinciannya di
# docs/07-environment-variables.md §2.10.
# Setup: Cloudflare dashboard -> R2 -> buat bucket -> Manage API Tokens.
# NEXT_PUBLIC_R2_PUBLIC_URL adalah domain publik bucket (custom domain atau
# r2.dev), dipakai untuk menyusun URL gambar yang tersimpan.
# ----------------------------------------------------------------------------
# R2_ACCOUNT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# R2_ACCESS_KEY_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# R2_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# R2_BUCKET_NAME=ecommerce-hns
# NEXT_PUBLIC_R2_PUBLIC_URL=https://media.hnsitcenter.com
# ^ perhatikan .com, BUKAN .id — media.hnsitcenter.id tidak terdaftar di DNS
#   (diverifikasi 18 Agustus 2026: "Name or service not known"). Domain ini
#   wajib cocok dengan remotePatterns di next.config.ts.


# ----------------------------------------------------------------------------
# [OPSIONAL] Admin Panel — AI untuk merapikan spesifikasi produk
# Dipakai /api/admin/format-specs: mengubah teks spesifikasi berantakan hasil
# copy-paste jadi tabel HTML. Tanpa ini tombolnya membalas error 500 yang rapi.
# ----------------------------------------------------------------------------
# GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
# GEMINI_API_KEY=xxxxxxxxxxxxxxxxxxxx


# ----------------------------------------------------------------------------
# [OPSIONAL] Sakelar pendaftaran manual (email+password)
# "true" membuka /register; selain itu (termasuk kalau variabel ini tidak ada
# sama sekali) pendaftaran manual TERTUTUP — default sengaja aman.
#
# Tiga lapis ikut mati saat tertutup: tautan "Daftar" di /login hilang, rute
# /register redirect ke /login, dan registerAction menolak di sisi server.
# Kirim-ulang verifikasi TETAP hidup, supaya orang yang sudah terlanjur
# mendaftar tidak terkunci di akun yang belum aktif.
#
# TANPA prefiks NEXT_PUBLIC_ dan itu disengaja: nilainya hanya dibaca di sisi
# server, jadi mengubahnya cukup RESTART proses Node — tidak perlu build ulang
# (bandingkan NEXT_PUBLIC_SITE_URL yang dibekukan saat build).
#
# Google OAuth tidak terpengaruh: masuk pertama kali lewat Google sekaligus
# mendaftarkan akun, dan jalur itu tidak butuh email verifikasi.
#
# Dibuat 18 Agustus 2026 sebagai langkah c3 runbook cutover: SMTP produksi
# (noreply@hnsitcenter.id) belum terbukti jalan, dan pendaftar yang emailnya
# tidak terkirim menghasilkan akun tak-terverifikasi yang hanya bisa
# diaktifkan lewat UPDATE SQL manual ke database produksi.
# ----------------------------------------------------------------------------
# REGISTER_MANUAL_ENABLED=false
